algoritmo "Atividade 67"
// Descrição : Algoritmo que inicia uma contagem de 0 até o número que o usuário digitar.

var
    n, contador: inteiro

inicio
// Entrada de dados
        escreval("Digite um número: ")
        leia(n)

// Contando de 0 até o número que o usuário digitou
        para contador <- 0 ate n passo 1 faca
            escreva(contador,", ")
        fimpara

fimalgoritmo