algoritmo "Contando números pares"

var
    cont, v: inteiro

inicio
    escreval("Digite um valor: ")
    leia(v)

    para cont <- 0 ate v passo 2 faca
        escreval(cont)
    fimpara

fimalgoritmo